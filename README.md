to run the project :
	go to game/target
	run the command "java -jar game-0.0.1-SNAPSHOT.jar"
	IN THE CASE WHERE THE JAR DOES NOT EXIT :
		run "mvn package"
		then run the previous command
to play the game :
	check out manual.txt